# index.css

I've been building __index.css__ to experiment with different CSS and Sass methods, it's still under going development but I wanted to set it up publically to also experiment with CDN so I've shared it as version 0 release.

Keeping that in mind the system is stable and works as intended and is shared as open source ready to use, albeit it currently without documentation.
